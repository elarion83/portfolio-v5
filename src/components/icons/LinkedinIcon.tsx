import React from 'react';
import { Linkedin } from 'lucide-react';

const LinkedinIcon: React.FC<{ className?: string; 'aria-hidden'?: boolean }> = (props) => {
  return <Linkedin {...props} />;
};

export default LinkedinIcon; 